@model IEnumerable<AprajitaRetails.Models.ViewModels.IERVM>

@{
    ViewData["Title"] = "IEReport";
}

<h1>IEReport</h1>

<p>
   <a asp-action="Create" class="btn btn-outline-light-green" id="create" data_modal="">Create New</a>
</p>
<table class="table table-bordered table-hover table-striped tables-sm">
    <thead class="bg-success">
        <tr>
            <th></th>
        </tr>
    </thead>
   <tbody class="bg-white">
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
