@model PaginatedList<AprajitaRetails.Models.AccountNumber>

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-danger">Index</h1>

<p>
    <a class="btn btn-outline-light-green btn-sm" asp-action="Create" id="create" data_modal="">Create</a>
</p>
<table class="table table-sm table-bordered table-hover table-striped">
     <thead class="bg-success">
        <tr>
            <th>
               @Html.DisplayNameFor(model => model.FirstOrDefault().Bank)
            </th>
            <th>
               @Html.DisplayNameFor(model => model.FirstOrDefault().Account)
            </th>
            <th></th>
        </tr>
    </thead>
     <tbody class="bg-white">
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Bank.BankId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account)
            </td>
            <td>
               <a  class="btn btn-outline-amber" id="edit" data_moda="" asp-action="Edit" asp-route-id="@item.AccountNumberId">Edit</a> |
               <a  class="btn btn-outline-info" id="detail" data_moda="" asp-action="Details" asp-route-id="@item.AccountNumberId">Details</a> |
               <a  class="btn btn-outline-danger" id="delete" data_moda="" asp-action="Delete" asp-route-id="@item.AccountNumberId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<partial name="_ModalPop" />
